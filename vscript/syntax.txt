# Defining variables
var number: int = 5

# script classes
class NewScript {
    var speed: float = 15.5
    var transform: Transform

    constructor() {}

    func OnStart() {
        transform = GetComponent<Transform>()
    }

    func OnUpdate() {
        transform.position.x = speed * Main.deltaTime;
    }

    func OnGUI() {

    }
}

# function declaration 
func Name(): void {
    Debug.Log("hi gang gang");
}

# class declaration

# Structures
struct Vector3 {
    var x: float = 0;
    var y: float = 0;
    var z: float = 0;
}

# C++ Internal calls
internal cpp_getkey(var: string) -> void
